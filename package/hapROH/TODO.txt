### TODO in hapROH:

Preprocessing:
* some filtering on ref_panel ? (I removed the options `n_ref` and `meta_path_ref`)
* does it work if the sample names contain only digits (in the sample eigenstrat file) ?
* load GenomicData from vcf

Post-procesing:
*** merging final dataframes

Plotting:
** compatibility with old plotting functions / new plotting fuctions

Testing:
**** compare outputs with previous version: see hapROH/Notebooks/Tests/hapROH_test_leipzig.ipynb
* compare time performance of python/cython/numba (on large dummy data ?)
* check memory requirements: ~nb_snp*2*nb_ref*9*nb_samples bytes (dominated by post_pb)
* check accuracy, especially downsampling experiments on WGS data

Handling missing values:
* Verify behaviour for missing data (necessary when processing multiple individuals at the same time)
* Verify math
* How are they stored and plotted

Documentation:
*** create example vignetes
* README_DEV with package structure
* sphinx documentation

Not implemented yet:
*** emission model diploid_gt
*** hapCON → see first how it is implemented in hapsburg → add emission model
- ROH on chrom X (female only) -> specific ref_panel -> change preprocessing
- lowmem: adapt to use ref panel stored as a packed bit array -> would divide reference loading time by 8
